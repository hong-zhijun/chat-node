# ────────────────────────────────────────────────────────────────────
# Nodex 私聊系统 · 环境变量配置示例
# 使用方法：复制本文件为 .env，填入真实值后启动 docker compose
#   cp .env.example .env
# ────────────────────────────────────────────────────────────────────

# ── 必须修改的安全配置 ──────────────────────────────────────────────
# 管理员登录密钥（建议 32 位以上随机字符串）
# 生成命令：openssl rand -hex 32
ADMIN_KEY=请替换为长随机字符串

# JWT 签名密钥（建议 64 位以上随机字符串）
# 生成命令：openssl rand -hex 64
JWT_SECRET=请替换为另一个长随机字符串

# ── 可选配置 ────────────────────────────────────────────────────────
JWT_EXPIRES_IN=30d
